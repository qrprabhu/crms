# Use relative /api when frontend and backend are routed under the same domain.
VITE_API_BASE_URL=/api

# If backend is on a separate domain, use full URL (uncomment and edit).
# VITE_API_BASE_URL=https://your-backend-name.ondigitalocean.app/api